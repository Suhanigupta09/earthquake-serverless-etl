version: 0.2

phases:
  build:
    commands:
      - pip install -r requirements.txt || true
      - python -m py_compile lambda_function.py
      - echo "Build completed"

artifacts:
  files:
    - lambda_function.py
    - requirements.txt